<%= link_to(spot_votes_path(spot), remote: true, method: :post) do %>
  <span class="vote voted">
    <i class="icon-good4"></i>
    <span class="count">
      <%= spot.votes_count %>
    </span>
  </span>
<% end %>
